- change setup phase to use rumble api key (when openid identifiers are available)
- need edit icon in task bubbles
- fix javascript weirdness
- authorization (?)
  - must be logged in to change anything
  - allow any user to set tasks for users who don't register
  - only allow current user to change his/her tasks
  - allow user to force task change for other user with a warning (?)
